# FLOW.md: affaan-m/ecc

> Routes 200 skills from `affaan-m/ECC` so the right one fires at the right phase.
>
> The stored index is at a cap, so this plugin ships more than are listed here.

## Routing

**The rule (MANDATORY, not advisory):** when a trigger matches you INVOKE the named skill with the Skill tool BEFORE producing anything. Naming a skill without calling it is not invoking.

```
USER MESSAGE
  ├─ about to commit changes to a JavaScript project that uses conventional commit messages?  → invoke ecc:everything-claude-code   gate: the commit message follows the conventional commits format used by the project
  ├─ about to implement or audit a user interface so people using assistive technology can use it?  → invoke ecc:accessibility   gate: the component was checked against WCAG 2.2 Level AA success criteria
  ├─ about to diagnose why an agent or LLM application is misbehaving across its whole stack rather than one function?  → invoke ecc:agent-architecture-audit   gate: a severity-ranked findings report spanning the agent stack layers exists
  ├─ about to compare several coding agents against each other on the same custom task?  → invoke ecc:agent-eval   gate: a table of pass rate, cost, time, and consistency exists per agent
  ├─ about to design or refine the tools, action space, or observation format an agent will use?  → invoke ecc:agent-harness-construction   gate: the tool definitions and observation format are documented and reviewed for completion rate
  ├─ about to debug why an AI agent itself failed a task rather than why the produced code failed?  → invoke ecc:agent-introspection-debugging   gate: a report with capture, diagnosis, and contained recovery steps exists
  ├─ about to let an AI agent autonomously spend funds through an on-chain payment protocol?  → invoke ecc:agent-payment-x402   gate: per-task spending limits and wallet controls are configured before the agent can transact
  ├─ just finished a non-trivial task and about to report it as complete?  → invoke ecc:agent-self-evaluation   gate: a 1 to 5 scorecard across accuracy, completeness, clarity, actionability, and conciseness exists
  ├─ deciding which parts of a large installed skill set should load by default for one specific project?  → invoke ecc:agent-sort   gate: each item is sorted into a daily or library bucket for the repo, backed by evidence
  ├─ about to start a substantial engineering task and choosing how to decompose it and which model tier to route it to?  → invoke ecc:agentic-engineering   gate: the task is decomposed with an eval defined and a model tier chosen by weighing cost against complexity
  ├─ about to design a multi-agent system that must persist state and run scheduled work across sessions without an external database?  → invoke ecc:agentic-os   gate: a kernel architecture with specialist agents, memory files, and scheduled jobs is documented
  ├─ about to define how a team should run its engineering process when AI agents write most of the implementation?  → invoke ecc:ai-first-engineering   gate: an operating model document for AI-majority implementation exists
  ├─ testing generated code where the same model that wrote it might also be asked to review it?  → invoke ecc:ai-regression-testing   gate: a regression suite runs in a sandboxed mode independent of the live database
  ├─ about to structure or review the module layout of an Android or Kotlin Multiplatform app?  → invoke ecc:android-clean-architecture   gate: module boundaries separate use cases, repositories, and the data layer per clean architecture
  ├─ about to write or architect Angular components, services, or routing?  → invoke ecc:angular-developer   gate: the generated Angular code follows current signals and dependency injection conventions
  ├─ about to add one more third-party API integration to a codebase that already has existing integrations?  → invoke ecc:api-connector-builder   gate: the new connector follows the same structural pattern as the existing integrations with no second pattern introduced
  ├─ about to design a new REST API endpoint or resource shape?  → invoke ecc:api-design   gate: the endpoint spec fixes naming, status codes, pagination, and versioning before implementation
  ├─ just made or about to make a significant architectural decision worth remembering later?  → invoke ecc:architecture-decision-records   gate: an entry recording context, alternatives, and rationale exists in the decision log
  ├─ about to write a long-form article, guide, or post that must read in an already-supplied voice?  → invoke ecc:article-writing   gate: the draft matches the supplied voice examples in tone before publishing
  ├─ need to know which automations, hooks, or connectors are actually live versus broken or redundant before changing any of them?  → invoke ecc:automation-audit-ops   gate: an inventory exists marking each automation live, broken, redundant, or missing evidence
  ├─ about to build a fully self-directed agent system instead of reaching for an external framework?  → invoke ecc:autonomous-agent-harness   gate: persistent memory, scheduled jobs, and a task queue are wired using native features only
  ├─ designing the control flow of a loop that runs an agent repeatedly without a human between steps?  → invoke ecc:autonomous-loops   gate: the loop architecture, sequential or multi-agent, is diagrammed before it is built
  ├─ about to architect the server side of an application beyond the shape of one single endpoint?  → invoke ecc:backend-patterns   gate: the backend design follows documented architecture and database optimization patterns for the stack
  ├─ have an already-scoped, tiered set of competitors and need to score them against each other on standard dimensions?  → invoke ecc:benchmark-methodology   gate: each competitor has a score across the nine weighted dimensions
  ├─ trying many implementation variants of the same thing and keeping the fastest or cheapest by repeated measurement?  → invoke ecc:benchmark-optimization-loop   gate: a table ranks each variant by measured latency, throughput, or cost
  ├─ checking whether a pull request changed performance compared to its baseline?  → invoke ecc:benchmark   gate: a before and after measurement exists comparing the same performance test
  ├─ verifying whether a Blender character rig or animation retarget is actually correct when a screenshot alone cannot tell?  → invoke ecc:blender-motion-state-inspection   gate: pose, ground contact, and facing direction are checked against the source motion data, not only rendered images
  ├─ turning a one-line project objective into a multi-session plan that other fresh agents will execute later cold?  → invoke ecc:blueprint   gate: each step carries a self-contained context brief and the plan passed an adversarial review gate
  ├─ helping a brand figure out its identity from nothing through structured interview questions across multiple sessions?  → invoke ecc:brand-discovery   gate: all eight discovery modules have documented answers
  ├─ building a reusable voice profile from existing real writing samples before any specific content gets drafted?  → invoke ecc:brand-voice   gate: a documented profile derived from source samples exists for other drafts to reference
  ├─ verifying that a newly deployed feature actually works by driving a real browser instead of only reading the code?  → invoke ecc:browser-qa   gate: a browser automation run recorded pass or fail against the deployed feature
  ├─ deciding whether to use Bun or Node as the runtime, package manager, or test runner for a project?  → invoke ecc:bun-runtime   gate: the choice between Bun and Node is written down with the reason and any deployment compatibility check
  ├─ just deployed, merged, or upgraded a dependency and need to confirm the live URL is still healthy?  → invoke ecc:canary-watch   gate: HTTP endpoints, static assets, and console errors were checked against the live URL after the change
  ├─ negotiating freight rates or evaluating how a carrier is performing for a logistics portfolio?  → invoke ecc:carrier-relationship-management   gate: a scorecard or RFP evaluation backs the decision
  ├─ about to review or write a Cisco IOS configuration change such as an ACL or interface setting?  → invoke ecc:cisco-ios-patterns   gate: the config change was checked against show command output before the change window closed
  ├─ starting a new session on a project and needing prior context to load automatically instead of being re-explained?  → invoke ecc:ck   gate: project context and prior session history load without a manual recap
  ├─ running several coding agents in parallel on isolated worktrees for one project and needing to track their progress centrally?  → invoke ecc:claude-devfleet   gate: a structured report summarizes each parallel worktree agent progress
  ├─ systematic debugging found nothing wrong yet users still report the interface ends up broken after a sequence of clicks?  → invoke ecc:click-path-audit   gate: every step of the click sequence and its resulting state is traced end to end
  ├─ about to write or optimize a ClickHouse query for an analytical workload?  → invoke ecc:clickhouse-io   gate: the query plan was checked against high-performance patterns before shipping
  ├─ explaining how a piece of code works to someone else as a persisted step-by-step walkthrough rather than a one-off answer?  → invoke ecc:code-tour   gate: a walkthrough exists with real file and line anchors for each step
  ├─ just joined an unfamiliar codebase and need a structured map of it before making changes?  → invoke ecc:codebase-onboarding   gate: a guide with an architecture map and a starter conventions file exists for the repo
  ├─ about to edit a file and wanting to know its structural quality score before and after the change?  → invoke ecc:codehealth-mcp   gate: a structural score delta from the review tool is recorded across the edit
  ├─ reviewing code for baseline naming, readability, or immutability conventions that apply regardless of framework?  → invoke ecc:coding-standards   gate: the review checks naming, readability, and immutability against the baseline conventions
  ├─ figuring out who actually counts as a competitor and which tier they belong to before any scoring begins?  → invoke ecc:competitive-platform-analysis   gate: a tiered, filtered competitor set with a source list exists
  ├─ already have scored competitor profile cards and need to turn them into one final decision-grade report?  → invoke ecc:competitive-report-structure   gate: a report exists with a landscape map, comparison matrix, white space analysis, and recommendations
  ├─ about to write or review Compose UI code for a Kotlin Multiplatform project?  → invoke ecc:compose-multiplatform-patterns   gate: state management and navigation follow the documented multiplatform UI conventions
  ├─ wondering whether the local agent configuration has accumulated stale or orphaned skills, hooks, or memory that should be removed?  → invoke ecc:config-gc   gate: each candidate item for deletion was confirmed individually before removal
  ├─ about to install, update, or reconfigure this plugin itself inside a specific AI coding tool?  → invoke ecc:configure-ecc   gate: the configuration steps match the real plugin and hook capabilities of that specific tool
  ├─ about to clean up or grow a social network following list toward current priorities?  → invoke ecc:connections-optimizer   gate: add, follow, and prune recommendations were reviewed before any action was taken
  ├─ about to turn one source asset into native content pieces and a calendar across several different platforms?  → invoke ecc:content-engine   gate: a platform-specific draft or calendar entry exists for each target platform
  ├─ about to cache the result of an expensive file processing step and worried about repeating the same work?  → invoke ecc:content-hash-cache-pattern   gate: cache key is a hash of file content, not its path
  ├─ wondering whether loaded skills, agents, and connectors are eating too much of the context window?  → invoke ecc:context-budget   gate: a ranked list of token-heavy or redundant components is produced
  ├─ designing an agent meant to run indefinitely rather than complete once and stop?  → invoke ecc:continuous-agent-loop   gate: the design names its quality gates and its recovery path on failure
  ├─ a session just ended and a lesson from it should be captured for this project specifically?  → invoke ecc:continuous-learning-v2   gate: a new project-scoped instinct with a confidence score is written
  ├─ about to invoke the retired first-version session-learning tool by its old name?  → invoke ecc:continuous-learning   gate: the request is redirected to the current successor instead of running the retired tool
  ├─ two or more services need to agree on an API or event shape before either side starts coding against it?  → invoke ecc:contract-first   gate: a versioned schema both sides implement against exists before integration begins
  ├─ building a pipeline that calls language models repeatedly and needs cheap tasks on cheap models?  → invoke ecc:cost-aware-llm-pipeline   gate: the pipeline picks a model tier per task and tracks spend against a budget
  ├─ asked how many tokens or dollars a session or model has actually burned?  → invoke ecc:cost-tracking   gate: a spend breakdown by model, session, or date comes from the local metrics log
  ├─ facing a decision with several defensible options and no clear best answer yet?  → invoke ecc:council   gate: at least two opposing positions are argued before a recommendation is made
  ├─ writing or reviewing implementation code in a C++ codebase and unsure which modern idiom applies?  → invoke ecc:cpp-coding-standards   gate: the change follows the cited core guidelines for naming, ownership, and safety
  ├─ a C++ test is flaky, missing, or the test runner needs configuring?  → invoke ecc:cpp-testing   gate: the suite runs deterministically with coverage or sanitizers noted
  ├─ the same piece of content needs to go out across several social platforms at once?  → invoke ecc:crosspost   gate: each platform gets an adapted version, none byte-identical to another
  ├─ writing or fixing a test in a C# project and need the right mocking or assertion library?  → invoke ecc:csharp-testing   gate: the test uses the assertion and mocking libraries already used elsewhere in the suite
  ├─ helping one specific customer with their subscription, refund, or billing-portal problem?  → invoke ecc:customer-billing-ops   gate: the customer subscription state is inspected through the connected billing tool before any change
  ├─ a shipment or product needs a tariff classification or a restricted-party screening before crossing a border?  → invoke ecc:customs-trade-compliance   gate: a classification code and a compliance check are recorded before the shipment proceeds
  ├─ building a mobile screen or state layer in a Flutter codebase and unsure which state-management approach to follow?  → invoke ecc:dart-flutter-patterns   gate: the new code matches the state-management framework already used in the app
  ├─ turning raw metrics into a monitoring view meant to answer a specific operator question?  → invoke ecc:dashboard-builder   gate: each panel maps to a named question an operator would actually ask
  ├─ a public data source needs to be collected on a recurring schedule and enriched automatically?  → invoke ecc:data-scraper-agent   gate: a scheduled job runs unattended and writes enriched results to the target store
  ├─ a bulk data load or export is too slow and needs to run much faster without corrupting records?  → invoke ecc:data-throughput-accelerator   gate: the faster path is measured against the original and matches its row counts
  ├─ about to change a live table schema and need it to roll forward and back safely?  → invoke ecc:database-migrations   gate: the schema change ships with a tested rollback and no downtime
  ├─ a topic needs a thorough, multi-source answer with citations rather than a quick lookup?  → invoke ecc:deep-research   gate: the final report cites the specific source each claim came from
  ├─ reviewing a smart contract that holds a liquidity pool or swap logic before it goes live?  → invoke ecc:defi-amm-security   gate: reentrancy, price oracle manipulation, and integer math are each explicitly checked
  ├─ about to declare a task finished before the checks that block completion have actually run?  → invoke ecc:delivery-gate   gate: no rationalization pattern, stale log, or disk issue is present at the stop
  ├─ setting up how a web application gets built, containerized, and rolled out to production?  → invoke ecc:deployment-patterns   gate: a health check and a rollback path exist before the first release
  ├─ reviewing a pull request that changes visual styling and need it to stay consistent with the rest of the product?  → invoke ecc:design-system   gate: the changed component reuses existing shared style tokens instead of new ones
  ├─ a Django project needs a background job or a task scheduled to run later rather than inline in the request?  → invoke ecc:django-celery   gate: the task runs through an async worker queue with retries configured
  ├─ laying out the architecture, routes, or ORM models of a Django project?  → invoke ecc:django-patterns   gate: the structure follows the layered convention already used for models, views, and serializers
  ├─ adding authentication, authorization, or user input handling in a Django project?  → invoke ecc:django-security   gate: CSRF protection and input sanitization are verified on the changed endpoint
  ├─ about to write implementation code for a Django feature before a failing test exists?  → invoke ecc:django-tdd   gate: a failing test exists first and drives the implementation
  ├─ about to open a pull request or cut a release for a Django project?  → invoke ecc:django-verification   gate: migrations, lint, tests with coverage, and a security scan all pass first
  ├─ coordinating several agent sessions running in parallel panes across different coding harnesses?  → invoke ecc:dmux-workflows   gate: each pane maps to a distinct task with no two panes editing the same files
  ├─ writing or reviewing a container definition or a multi-service compose file?  → invoke ecc:docker-patterns   gate: the build is reproducible and each service is isolated on its own network
  ├─ about to write code against a library API from memory instead of checking current docs?  → invoke ecc:documentation-lookup   gate: the API usage is confirmed against freshly fetched documentation
  ├─ structuring dependency injection or async and await code in a C# project targeting .NET?  → invoke ecc:dotnet-patterns   gate: the change follows the dependency-injection and async conventions already used in the project
  ├─ a one-off task needs its own temporary harness and success gate rather than a general-purpose loop?  → invoke ecc:dynamic-workflow-mode   gate: the harness defines a machine-checkable success condition before it runs
  ├─ writing a browser test that drives the whole application through the UI rather than one unit in isolation?  → invoke ecc:e2e-testing   gate: the test locates elements through a page object and runs in the CI pipeline
  ├─ a user is asking what this plugin offers or how to get started with it in general?  → invoke ecc:ecc-guide   gate: the answer is read from the live repository surface rather than recalled from memory
  ├─ a user describes a workflow and wants to know which group of commands to run and in what order?  → invoke ecc:ecc-recipes   gate: a named command group with a run order and a stop condition is returned
  ├─ billing for the tooling repo itself spiked and the cause might be runaway automation rather than real usage?  → invoke ecc:ecc-tools-cost-audit   gate: the spike is traced to a specific job, duplicate run, or model tier before anything changes
  ├─ asked to triage, draft, or confirm delivery of an actual email rather than draft example copy?  → invoke ecc:email-ops   gate: what was actually sent is confirmed by checking the sent folder, not assumed
  ├─ evaluating an electricity or gas supply contract, tariff, or renewable power agreement for a facility?  → invoke ecc:energy-procurement   gate: the recommendation compares tariff structures against the actual demand profile of the facility
  ├─ an agent workload needs to keep running for a long time in production with its own security boundaries?  → invoke ecc:enterprise-agent-ops   gate: the long-running workload has observability and a defined stop or restart lifecycle
  ├─ writing code that can fail at runtime and needs a typed error path instead of an unhandled throw?  → invoke ecc:error-handling   gate: failures are typed and either retried, boundaried, or surfaced with a clear message
  ├─ a coding session needs a formal, repeatable scorecard rather than an informal impression of how it went?  → invoke ecc:eval-harness   gate: a structured evaluation run produces a repeatable score
  ├─ handling a token amount on a chain and unsure whether its decimal precision was looked up correctly?  → invoke ecc:evm-token-decimals   gate: the decimal count is read from the chain at runtime rather than assumed
  ├─ need a neural web, code, or company lookup rather than a keyword search?  → invoke ecc:exa-search   gate: results come back through the neural search connector with sources attached
  ├─ asked to generate an image, video, or audio clip through a hosted generation API?  → invoke ecc:fal-ai-media   gate: the generated media comes back in the requested modality from the hosted connector
  ├─ structuring endpoints, request validation, or dependency injection in a Python async web service?  → invoke ecc:fastapi-patterns   gate: request and response bodies are validated with typed schemas instead of raw dicts
  ├─ asked for the actual revenue, pricing, or refund numbers behind this business rather than generic payments advice?  → invoke ecc:finance-billing-ops   gate: the answer is backed by real billing records instead of general guidance
  ├─ setting up a reproducible toolchain or system dependency for a project across mac and linux?  → invoke ecc:flox-environments   gate: the environment is declared in a manifest that reproduces identically on another machine
  ├─ about to review Flutter or Dart widget code before it merges?  → invoke ecc:flutter-dart-code-review   gate: a completed checklist covers widget structure, state management choice, and accessibility
  ├─ building an iOS feature that must run a language model fully on device without a network call?  → invoke ecc:foundation-models-on-device   gate: a working on device text generation call succeeds using the built in model API
  ├─ building an interactive web component or form and unsure it works with a keyboard or screen reader?  → invoke ecc:frontend-a11y   gate: keyboard navigation and screen reader labeling are verified on the component
  ├─ about to start building a website, dashboard, or app screen with no agreed visual direction yet?  → invoke ecc:frontend-design-direction   gate: a written design direction exists before the first component is coded
  ├─ implementing a React or Next.js feature and unsure which state management or performance approach fits?  → invoke ecc:frontend-patterns   gate: the implementation follows a named documented pattern rather than an ad hoc choice
  ├─ asked to build a talk or pitch deck as an animated web page instead of a slide document?  → invoke ecc:frontend-slides   gate: a rendered slide deck exists as a web page
  ├─ writing or fixing a test in an F# codebase?  → invoke ecc:fsharp-testing   gate: the test file matches the project test structure and passes
  ├─ designing an autonomous system where one agent generates candidate work and a second agent scores it before more work continues?  → invoke ecc:gan-style-harness   gate: a generator role and a separate evaluator role are both defined with a pass or fail criterion
  ├─ about to edit a file or run a command without having checked the real data schema, import paths, or explicit instruction first?  → invoke ecc:gateguard   gate: the relevant importers, schemas, or instruction are inspected and cited before the change is made
  ├─ packaging a Python application into a small fast Windows installer and the default build is too large or slow to start?  → invoke ecc:generating-python-installer   gate: a compiled distribution shows a measured size and startup improvement over the default build
  ├─ deciding how to branch, name commits, or resolve a merge conflict on a team project?  → invoke ecc:git-workflow   gate: a stated branching and commit convention is followed for the change
  ├─ needing to triage issues, manage pull requests, or check CI status directly against a repository?  → invoke ecc:github-ops   gate: repository state changes through a real command line call rather than a manual description
  ├─ writing new Go application code and unsure what the idiomatic structure looks like?  → invoke ecc:golang-patterns   gate: the code follows the conventional project layout and idioms for the language
  ├─ writing or fixing a test for Go code specifically?  → invoke ecc:golang-testing   gate: a table driven test exists and passes with the standard test runner
  ├─ needing to find, edit, or clean up a doc, sheet, slide, or drive file as part of a task?  → invoke ecc:google-workspace-ops   gate: the target file is actually opened and changed through the real cloud office tool rather than described only
  ├─ just finished a complex task or failure and about to move on without capturing the reusable lesson?  → invoke ecc:growth-log   gate: a written entry captures a reusable pattern from the task, not a diary style note
  ├─ building a feature that checks a drug interaction, validates a dose, or computes a clinical severity score?  → invoke ecc:healthcare-cdss-patterns   gate: the alert or score logic matches a named clinical scoring standard
  ├─ building an encounter workflow, prescription screen, or clinical data entry form for a medical record system?  → invoke ecc:healthcare-emr-patterns   gate: the workflow follows a medical record specific accessibility and safety checklist
  ├─ about to deploy a healthcare application and unsure its safety test suite has actually run?  → invoke ecc:healthcare-eval-harness   gate: automated accuracy and data exposure tests pass before deployment proceeds
  ├─ handling patient health data and unsure how it should be classified, encrypted, or access controlled?  → invoke ecc:healthcare-phi-compliance   gate: the data path has a documented classification, access control, and encryption approach
  ├─ preparing a private local automation workflow for public release and worried it still contains credentials or local paths?  → invoke ecc:hermes-imports   gate: the exported artifact contains no credentials, workspace state, or local only paths
  ├─ designing or refactoring a service and unsure how to keep domain logic independent of frameworks and databases?  → invoke ecc:hexagonal-architecture   gate: domain logic depends only on defined ports, with adapters implementing them at the edges
  ├─ a task is explicitly framed around a covered entity, a business associate agreement, or a breach notification duty?  → invoke ecc:hipaa-compliance   gate: the regulatory requirement is checked against the specific named legal obligation, not general privacy practice
  ├─ about to change a home router, firewall, DHCP, or VPN configuration without a pre change checklist?  → invoke ecc:homelab-network-readiness   gate: segmentation, DNS filtering, and remote access are all confirmed ready before the config change
  ├─ planning a home network from scratch and unsure how to lay out gateways, switches, and IP ranges?  → invoke ecc:homelab-network-setup   gate: a network plan exists covering addressing, DHCP reservations, and cabling before purchase
  ├─ setting up or troubleshooting network wide ad blocking at the DNS level on a home network?  → invoke ecc:homelab-pihole-dns   gate: DNS queries are confirmed resolving through the blocklist service
  ├─ splitting a home network into separate broadcast domains for IoT, guest, and trusted devices?  → invoke ecc:homelab-vlan-segmentation   gate: each device class sits on its own tagged network with a firewall rule between them
  ├─ setting up encrypted remote access into a home network from a phone or laptop specifically?  → invoke ecc:homelab-wireguard-vpn   gate: a peer configuration connects and routes traffic through the encrypted tunnel
  ├─ asked to create or configure a new automation rule in a rule engine specific syntax?  → invoke ecc:hookify-rules   gate: a rule file matches that engine specific syntax and validates
  ├─ onboarding an AI coding agent onto a hand written legacy codebase that has its own inconsistent conventions?  → invoke ecc:inherit-legacy-style   gate: new code matches the legacy project own idioms rather than generic modern defaults
  ├─ a requested change is ambiguous, high impact, or touches security or migration risk with no acceptance criteria yet?  → invoke ecc:intent-driven-development   gate: scoped, verifiable acceptance criteria exist before or alongside the implementation
  ├─ forecasting demand, setting safety stock, or planning replenishment across multiple retail locations?  → invoke ecc:inventory-demand-planning   gate: a forecast method is chosen and safety stock levels are computed per location
  ├─ needing to create or update a pitch deck, one pager, or financial projection document for fundraising?  → invoke ecc:investor-materials   gate: a fundraising document exists with internally consistent figures throughout
  ├─ needing to draft a cold email, warm intro, or follow up message to a potential investor?  → invoke ecc:investor-outreach   gate: a personalized outreach message is drafted for a specific named contact
  ├─ needing app icon image files created for an iOS Xcode asset catalog?  → invoke ecc:ios-icon-gen   gate: image files are produced at every required icon size for the asset catalog
  ├─ a subagent keeps running out of useful context because a first pass retrieved too much or too little?  → invoke ecc:iterative-retrieval   gate: context is gathered in progressively refined passes rather than one large fetch
  ├─ comparing a prediction market basket against your own existing notes, portfolio, or watchlist for gaps?  → invoke ecc:ito-basket-compare   gate: a read only comparison lists where the basket diverges from the existing position or thesis
  ├─ needing to find available GPU capacity or submit a compute rental request?  → invoke ecc:ito-compute   gate: an inventory query or rate request is executed through the real compute command line tool
  ├─ designing the architecture of a research agent for market discovery rather than running basket research directly?  → invoke ecc:ito-data-atlas-agent   gate: an agent design exists with source grounded retrieval, not yet wired to live execution
  ├─ GPU compute has already been booked and now checking whether a model serving endpoint is available on it?  → invoke ecc:ito-inference   gate: a confirmed serving manifest is handed off only once the endpoint is verified live
  ├─ researching market events, venues, or liquidity conditions rather than comparing to an existing portfolio?  → invoke ecc:ito-market-intelligence   gate: a source grounded briefing on the event or venue exists with citations
  ├─ drafting the steps and constraints for a possible trade without placing an actual order?  → invoke ecc:ito-trade-planner   gate: a worksheet lists venues, prerequisites, and manual steps with no order submitted
  ├─ GPU compute has already been booked and the user now wants to run a training or fine tuning job on it?  → invoke ecc:ito-training   gate: a training job is launched against the existing booking record
  ├─ writing Java code for a Spring Boot or Quarkus service and unsure of the project naming or exception conventions?  → invoke ecc:java-coding-standards   gate: the code matches the framework specific naming and exception conventions automatically
  ├─ needing to read, update, or transition a Jira ticket as part of the current task?  → invoke ecc:jira-integration   gate: the ticket status or comment changes through the real API rather than being described only
  ├─ designing an entity, relationship, or query for a Spring Boot data layer over a relational database?  → invoke ecc:jpa-patterns   gate: the entity mapping and query are reviewed for indexing and pagination correctness
  ├─ needing to save, deduplicate, or search notes across several different storage systems at once?  → invoke ecc:knowledge-ops   gate: the same fact is confirmed consistent across every storage layer it lives in
  ├─ writing concurrent code using structured coroutines or a reactive stream in a Kotlin codebase?  → invoke ecc:kotlin-coroutines-flows   gate: structured concurrency is used with a defined error handling path, and a test covers it
  ├─ writing database access code with a Kotlin specific SQL DSL and ORM library rather than raw JDBC?  → invoke ecc:kotlin-exposed-patterns   gate: queries go through typed DSL or DAO style code with pooled connections rather than raw SQL strings
  ├─ building a server route, plugin, or websocket handler using a Kotlin first web framework rather than Spring?  → invoke ecc:kotlin-ktor-patterns   gate: the route is registered through a routing DSL and covered by an in process test client call
  ├─ about to write Kotlin application code and unsure which idiomatic pattern to use for coroutines, null safety, or DSL builders?  → invoke ecc:kotlin-patterns   gate: the resulting Kotlin code follows idiomatic conventions for coroutines, null safety, and DSL builders
  ├─ about to add coverage for Kotlin coroutine code and unsure which mocking or property-based approach fits?  → invoke ecc:kotlin-testing   gate: a Kotlin test suite using the chosen framework and coroutine test utilities exists with coverage reported
  ├─ about to author or debug a Kubernetes manifest, RBAC rule, or workload resource?  → invoke ecc:kubernetes-patterns   gate: the manifest defines resource limits, probes, and RBAC scoped to the workload, and kubectl confirms the resource is healthy
  ├─ about to build or structure a Laravel feature such as a controller, an Eloquent model, a queue job, or an API resource?  → invoke ecc:laravel-patterns   gate: the feature is organized into controller, service layer, and Eloquent model following the framework convention
  ├─ looking for an existing Laravel package before writing custom code and need to check its health or compatibility?  → invoke ecc:laravel-plugin-discovery   gate: a shortlist of candidate packages with health and compatibility data pulled from the package directory exists
  ├─ about to implement authentication, authorization, or handle user input in a Laravel app and need to close a gap before it ships?  → invoke ecc:laravel-security   gate: CSRF protection, input validation, and authorization checks are present on the affected routes
  ├─ about to implement a Laravel feature and no failing test exists for it yet?  → invoke ecc:laravel-tdd   gate: a failing PHPUnit or Pest test exists before the implementation code is written
  ├─ about to ship or release a Laravel project and need a final readiness check across linting, tests, and security scans?  → invoke ecc:laravel-verification   gate: linting, static analysis, tests, and security scans all pass in one recorded run before release
  ├─ designing a system such as a realtime dashboard, streaming agent, or execution gateway where p95 freshness is the primary constraint?  → invoke ecc:latency-critical-systems   gate: the design states a measured p95 latency budget and the components chosen to meet it
  ├─ about to build or run a lead scoring and outreach pipeline across email, LinkedIn, or X?  → invoke ecc:lead-intelligence   gate: a ranked lead list with signal scores and a drafted outreach message per channel exists
  ├─ implementing a glass material, blur, or morphing surface effect in an iOS SwiftUI or UIKit interface?  → invoke ecc:liquid-glass-design   gate: the interface uses the platform glass material APIs rather than a custom blur effect
  ├─ building an autonomous agent that can move funds or sign transactions and need to bound what it can spend?  → invoke ecc:llm-trading-agent-security   gate: spend limits, a pre-send simulation step, and a circuit breaker are wired into the transaction path
  ├─ handling a shipment delay, damage claim, or carrier dispute and need the right escalation protocol?  → invoke ecc:logistics-exception-management   gate: the exception is logged against the correct escalation protocol with a claims procedure attached
  ├─ creating or reviewing an autonomous agent loop and need to check it cannot spin forever or game its own verifier?  → invoke ecc:loop-design-check   gate: the loop has a machine-decidable stop condition and a verifier that cannot be gamed by the loop itself
  ├─ implementing transactional email sending and need to verify delivery in a sandbox before the live domain is used?  → invoke ecc:mailtrap-email-integration   gate: a test email is confirmed delivered through the sandbox environment before the live domain is used
  ├─ reviewing a finished UI that looks functionally correct but still feels unpolished?  → invoke ecc:make-interfaces-feel-better   gate: specific spacing, hit-area, or interaction-state fixes are listed and applied against the reviewed screen
  ├─ asked to produce an animated technical explainer for a concept, graph, or system diagram?  → invoke ecc:manim-video   gate: a rendered scene file exists that animates the concept being explained
  ├─ asked for market sizing, competitor comparison, or industry intelligence with cited sources?  → invoke ecc:market-research   gate: the research output cites its sources for every sizing or comparison claim
  ├─ asked to plan a coordinated multi-channel push across landing copy, email, social, and ad copy under one angle?  → invoke ecc:marketing-campaign   gate: a single plan exists covering positioning, channel assets, and a content calendar
  ├─ about to build a new server that exposes tools, resources, or prompts over MCP and need to choose its transport?  → invoke ecc:mcp-server-patterns   gate: the server defines its tools and resources with schema validation and a chosen transport such as stdio or Streamable HTTP
  ├─ asked to check a text message or DM, or recover a one-time code, and need to prove which source was actually read?  → invoke ecc:messages-ops   gate: the retrieved message is quoted together with the source thread it came from
  ├─ asked to add a machine learning capability to a codebase that has none today?  → invoke ecc:ml-adoption-playbook   gate: the problem is framed, data readiness is assessed, and a baseline model is integrated behind a decoupled interface
  ├─ building or hardening a production machine learning system beyond a one-off notebook?  → invoke ecc:mle-workflow   gate: the system has a data contract, a reproducible training run, and a monitored deployment with a rollback path
  ├─ implementing a drag, gesture, or imperative animation sequence in React beyond a basic transition?  → invoke ecc:motion-advanced   gate: the advanced interaction reuses the shared motion tokens and springs rather than ad hoc animation values
  ├─ about to add any animation to a React or Next.js app and no shared tokens or spring presets exist yet?  → invoke ecc:motion-foundations   gate: shared motion tokens and spring presets are defined and referenced instead of inline animation values
  ├─ implementing a common UI moment such as a modal, toast, or page transition and need it to feel consistent with the rest of the app?  → invoke ecc:motion-patterns   gate: the component reuses the shared motion tokens and springs instead of inventing new easing or duration values
  ├─ asked to add motion or transitions to a React or Next.js interface and unsure which motion layer to start from?  → invoke ecc:motion-ui   gate: the chosen motion system is applied consistently across the affected components
  ├─ designing a MySQL schema, index, or transaction boundary for a production backend?  → invoke ecc:mysql-patterns   gate: the schema or query change includes the supporting index and is verified against the query plan
  ├─ operating or extending a session-aware REPL built on top of a headless agent runner?  → invoke ecc:nanoclaw-repl   gate: the REPL session preserves state across turns without an added external dependency
  ├─ structuring a new NestJS module, controller, or provider and need the DTO validation and guard conventions?  → invoke ecc:nestjs-patterns   gate: the module follows the controller-provider-DTO structure with validation and guards attached
  ├─ automating SSH collection or configuration changes across multiple network devices with Python?  → invoke ecc:netmiko-ssh-automation   gate: the script runs read-only by default, batches devices with timeouts, and gates any config change explicitly
  ├─ troubleshooting a BGP neighbor or route exchange problem and need to collect evidence without changing config?  → invoke ecc:network-bgp-diagnostics   gate: the diagnostic output captures neighbor state and AS path evidence with no configuration commands issued
  ├─ about to push a router or switch configuration change and need it checked for dangerous commands first?  → invoke ecc:network-config-validation   gate: the config change is checked for duplicate addresses, subnet overlap, and dangerous commands before it is pushed
  ├─ investigating errors, drops, or flapping on a device interface and need the counter trend read correctly?  → invoke ecc:network-interface-health   gate: the interface counters are read and the specific fault type, such as duplex mismatch or CRC errors, is identified
  ├─ deciding which bundler to use for a Next.js build, or debugging a slow dev server?  → invoke ecc:nextjs-turbopack   gate: the bundler choice is confirmed against measured dev server startup time
  ├─ hashing data for an Ethereum selector, signature, or storage slot in JavaScript or TypeScript?  → invoke ecc:nodejs-keccak256   gate: the hash function used is confirmed to match the Ethereum standard and not a generic SHA3 library default
  ├─ asked to convert, OCR, redact, or fill a document such as a PDF or DOCX programmatically?  → invoke ecc:nutrient-document-processing   gate: the document operation completes through the document API and the output file is produced in the target format
  ├─ building a page in a Nuxt app and need SSR-safe data fetching instead of a hydration mismatch?  → invoke ecc:nuxt4-patterns   gate: data fetching uses the SSR-safe composable and the page hydrates without a mismatch warning
  ├─ asked to create or design a full character identity file for a new agent platform persona, including its name and avatar prompt?  → invoke ecc:openclaw-persona-forge   gate: an identity file exists with character rules, a name, and an avatar prompt for the new agent
  ├─ asked to prepare a private project for public open-source release?  → invoke ecc:opensource-pipeline   gate: the released copy has been forked, scanned for private state or secrets, and packaged separately from the private original
  ├─ asked to add a capability that does not exist in the codebase yet and it needs the full research-to-commit sequence?  → invoke ecc:orch-add-feature   gate: research, a plan, a TDD implementation, and a review each completed before the commit is gated through
  ├─ handed a design or spec document and asked to turn it into a first running MVP slice?  → invoke ecc:orch-build-mvp   gate: the first end-to-end vertical slice from the spec is scaffolded, implemented under TDD, and reviewed before commit
  ├─ asked to change the behavior of a feature that already works correctly, to a new specified behavior?  → invoke ecc:orch-change-feature   gate: the tests are updated to the new spec first, then the implementation is changed to match them
  ├─ asked to fix a bug in existing behavior and no regression test reproduces it yet?  → invoke ecc:orch-fix-defect   gate: a failing regression test reproduces the bug before the fix is applied, and it passes after
  ├─ needing to change the shared engine that every feature-building or bug-fixing orchestration step delegates to, instead of running one of those steps directly?  → invoke ecc:orch-pipeline   gate: the pipeline stage, size classification, and human gate being changed are identified before any orchestration step is touched
  ├─ asked to restructure code without changing what it does, and tests are not yet confirmed green beforehand?  → invoke ecc:orch-refine-code   gate: tests are green before the restructuring starts and remain green after it, with no behavior change
  ├─ a task is taking long because independent pieces of it are being run one at a time?  → invoke ecc:parallel-execution-optimizer   gate: the independent pieces run concurrently in isolated worktrees or batched calls with results merged correctly
  ├─ writing Perl code and unsure whether it follows modern idioms rather than legacy style?  → invoke ecc:perl-patterns   gate: the Perl code uses modern signatures, feature pragmas, and idioms rather than legacy constructs
  ├─ handling untrusted input or shelling out to a process from Perl and need it done safely?  → invoke ecc:perl-security   gate: taint mode is on, database queries are parameterized, and the security linter passes
  ├─ about to write or run automated checks for Perl code and need a failing test in place before the implementation?  → invoke ecc:perl-testing   gate: a failing test for the Perl script exists under the prove runner before the implementation code is written
```

**Drift:** every route above targets `ecc:<slug>` in the separately-installed plugin. If a slug no longer resolves there, that route is a silent no-op. Never substitute a nearby-sounding skill: a broken route means this Flow needs an update, not that you may improvise.

## Attribution

Skills routed by this Flow come from **ecc** (https://github.com/affaan-m/ECC). This overlay bundles none of them; it installs the routing only.

Routing (this FLOW.md) by Flowy, CC-BY-SA-4.0.
