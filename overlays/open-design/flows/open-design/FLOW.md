# FLOW.md: nexu-io/open-design

> Routes all 163 skills from `nexu-io/open-design` so the right one fires at the right phase.

## Routing

**The rule (MANDATORY, not advisory):** when a trigger matches you INVOKE the named skill with the Skill tool BEFORE producing anything. Naming a skill without calling it is not invoking.

```
USER MESSAGE
  ├─ does the user want to contribute a new skill, translation, documentation fix or bug report back to this plugin itself?  → invoke open-design:od-contribute   gate: a pull request or issue has been opened against the plugin repository
  ├─ does the user want a multi-scene retro pixel-art animated video composition with transitions and interactive preview controls?  → invoke open-design:8-bit-orbit-video-template   gate: a multi-scene HTML-to-video composition with transitions and a working preview exists
  ├─ is the user asking for ad headlines, descriptions or primary text for a paid campaign?  → invoke open-design:ad-creative   gate: a set of ad creative variants with headline, description and primary text exists
  ├─ does the user want a moody dark high-fashion editorial style motion storyboard?  → invoke open-design:after-hours-editorial-template   gate: a three-page cinematic storyboard using dark editorial styling exists
  ├─ does the task require driving a real browser to click, fill forms or extract live page data?  → invoke open-design:agent-browser   gate: a browser automation command has run and returned page state or a screenshot
  ├─ is the user planning or producing a full length recorded music project from concept through final export?  → invoke open-design:ai-music-album   gate: a sequenced multi-track project with drafted lyrics and a final export exists
  ├─ does the user want procedurally generated visual art that can be reproduced from a fixed seed?  → invoke open-design:algorithmic-art   gate: a seeded generative sketch renders the same output for the same seed
  ├─ is the agent designing an interface for an Apple platform and needs to check official interface conventions?  → invoke open-design:apple-hig   gate: the design decisions cite the relevant platform guideline section
  ├─ does the user want plain notes or markdown turned into a polished long-form editorial essay page?  → invoke open-design:article-magazine   gate: a polished long-form HTML essay page exists from the source notes
  ├─ is the user asking for a complex multi-component interactive claude.ai page built with a modern frontend stack?  → invoke open-design:artifacts-builder   gate: a multi-component interactive page using the specified frontend stack renders without errors
  ├─ has the user shared a rough or vague idea before any concrete design direction has been chosen?  → invoke open-design:brainstorming   gate: a set of explored alternatives and clarifying questions exists before a direction is picked
  ├─ does the user want colors, fonts and a logo pulled automatically from an existing live website?  → invoke open-design:brand-extract   gate: a brand kit with extracted colors, fonts and logo assets exists from the live site
  ├─ does the artifact need to follow a specific official color and typography standard rather than a custom one?  → invoke open-design:brand-guidelines   gate: the artifact uses the specified official color and typography tokens consistently
  ├─ does the user want generated brand identity board images rather than a written guideline document?  → invoke open-design:brandkit   gate: generated brand board images covering logo, palette and identity exist
  ├─ does the user want a harsh rigid terminal-like visual style rather than a soft or minimal one?  → invoke open-design:brutalist-skill   gate: the layout uses a rigid grid, extreme type contrast and utilitarian color only
  ├─ does the user want a static poster or illustration exported as an image or PDF rather than an interactive page?  → invoke open-design:canvas-design   gate: a rendered static image or PDF art file exists
  ├─ does the user want a shareable quote or data card sized for a single social post?  → invoke open-design:card-twitter   gate: a single card image sized for a social post exists
  ├─ does the user want a swipeable multi-card knowledge carousel in a specific regional social style?  → invoke open-design:card-xiaohongshu   gate: a multi-card carousel set in that visual style exists
  ├─ does the user want a chat conversation turned into an animated overlay video from a transcript or screenshot?  → invoke open-design:chat-motion-overlay   gate: a rendered chat overlay bundle from the supplied transcript or screenshot exists
  ├─ does a design decision hinge on a color science question like contrast ratio or palette generation?  → invoke open-design:color-expert   gate: the chosen colors are justified by a specific contrast or color calculation
  ├─ does the user want to see what messaging and creative competitors are currently running in ads?  → invoke open-design:competitive-ads-extractor   gate: a set of extracted competitor ads with messaging analysis exists
  ├─ is the user asking for new marketing copy to be written for a page rather than a visual layout?  → invoke open-design:copywriting   gate: draft marketing copy for the requested page section exists
  ├─ does a creative concept need a structured multi-phase ideation and self-critique process before it is presented?  → invoke open-design:creative-director   gate: the concept has passed through a documented multi-phase evaluation before presentation
  ├─ does the task require an interactive code-driven data chart rather than a static image chart?  → invoke open-design:d3-visualization   gate: an interactive chart renders correctly against the supplied data
  ├─ has the user supplied raw spreadsheet or JSON data that needs to become a readable report page?  → invoke open-design:data-report   gate: a polished report page rendering the supplied dataset exists
  ├─ does the user want a slide deck styled like a muted paper editorial magazine rather than a bold digital one?  → invoke open-design:deck-guizang-editorial   gate: the deck uses one of the fixed muted-palette editorial layouts
  ├─ does the user want a slide deck built from freely composed components rather than a fixed template?  → invoke open-design:deck-open-slide-canvas   gate: a deck exists built from custom-composed components at a locked canvas size, not a preset layout
  ├─ does the user want a slide deck using a strict multi-column grid with exactly one saturated accent color and no template flexibility?  → invoke open-design:deck-swiss-international   gate: the deck follows a fixed multi-column grid with exactly one saturated accent color
  ├─ does the design work need a structured brief capturing goal, audience and constraints before starting?  → invoke open-design:design-brief   gate: a formatted brief document exists before design work begins
  ├─ is this a from-zero kickoff where no design system exists yet and one must be built end to end?  → invoke open-design:design-consultation   gate: a complete design system with realistic mockups exists where none existed before
  ├─ does the project need its design direction and tokens recorded in one canonical reference file?  → invoke open-design:design-md   gate: a single design reference file with tokens and visual rules exists and stays current
  ├─ does existing shipped UI need a visual audit and fixes before it goes live?  → invoke open-design:design-review   gate: before and after screenshots exist alongside atomic commits fixing the audited issues
  ├─ does the user want a fintech-style data deck with a strict grid and bold numeric cards in a single file?  → invoke open-design:digits-fintech-swiss-template   gate: a single-file navigable deck with a fixed fintech grid and numeric cards exists
  ├─ does the user want a document styled like warm parchment paper with a single serif typeface?  → invoke open-design:doc-kami-parchment   gate: the document uses a parchment background with one accent color and one serif family only
  ├─ does an existing Word-style document need to be read or edited while preserving its exact formatting and layout?  → invoke open-design:doc   gate: the edited document preserves the original formatting and layout fidelity
  ├─ does a Word-style document need tracked changes, comments or a full content analysis for review purposes?  → invoke open-design:docx   gate: the document shows tracked changes or comments ready for reviewer feedback
  ├─ does the user need new site name ideas checked for availability across multiple domain extensions?  → invoke open-design:domain-name-brainstormer   gate: a list of candidate site names with checked availability exists
  ├─ has the user uploaded real product photos that need to become main, feature and lifestyle shop-ready images?  → invoke open-design:ecommerce-image-workflow   gate: a compact set of product-faithful shop images derived from the uploaded photos exists
  ├─ does the user want a manifesto or culture deck in a dark red and gold editorial palette?  → invoke open-design:editorial-burgundy-principles-template   gate: the deck uses a fixed dark red palette with pill tags and principle cards
  ├─ is a UI component being judged or refined against a high polish bar covering more than just motion?  → invoke open-design:emil-design-eng   gate: the component review covers spacing, hierarchy and interaction details, not motion alone
  ├─ does a finished interface need a dedicated follow-up pass adding only motion and transitions?  → invoke open-design:emilkowalski-motion   gate: a motion-only follow-up pass was applied after the interface layout was already final
  ├─ is a request for visual output too vague to act on without more design vocabulary?  → invoke open-design:enhance-prompt   gate: the original request has been rewritten with concrete design specification terms
  ├─ is a file export or download producing an empty or failed file instead of the expected output?  → invoke open-design:export-download-debugging   gate: the export now produces a non-empty file where it previously failed or produced a zero-byte file
  ├─ does the user want an actual 3D model file generated from a text prompt or image?  → invoke open-design:fal-3d   gate: a generated 3D model file exists from the given input
  ├─ does the user need a high-quality standalone image or video generated from a hosted production model?  → invoke open-design:fal-generate   gate: a generated image or video file exists from the requested production model
  ├─ does an existing image need style transfer, background removal, object removal or inpainting applied to it?  → invoke open-design:fal-image-edit   gate: the edited image reflects the requested removal or transfer with the rest of the image unchanged
  ├─ has the user specifically asked for one named premium hosted model to generate an image or video?  → invoke open-design:fal-kling-o3   gate: a generated asset from that specific named model exists
  ├─ does the user want an existing face or avatar video synced to a separate audio track?  → invoke open-design:fal-lip-sync   gate: a synced video matching the supplied audio track exists
  ├─ does the user want many fast draft variations for moodboarding rather than one final high-quality render?  → invoke open-design:fal-realtime   gate: multiple rapid draft variations were generated in a single interactive session
  ├─ is an existing image degraded, blurry or damaged and needs quality restored rather than newly generated?  → invoke open-design:fal-restore   gate: the restored image shows measurably reduced blur, noise or damage versus the original
  ├─ does the user want a custom model trained on their own reference images for repeated reuse?  → invoke open-design:fal-train   gate: a trained custom model artifact exists that can be reused for future generations
  ├─ does the user want to preview a garment rendered onto a photo of a specific person?  → invoke open-design:fal-tryon   gate: a composited image shows the garment applied to the supplied photo of that person
  ├─ does an existing image or video need higher resolution than its current source file provides?  → invoke open-design:fal-upscale   gate: the output file has a measurably higher resolution than the input
  ├─ does an existing video file need its style, background or audio changed rather than a new video created?  → invoke open-design:fal-video-edit   gate: the edited video reflects the requested style, background or audio change from the source file
  ├─ does the task require extracting text, objects or answers from an existing image rather than creating one?  → invoke open-design:fal-vision   gate: an analysis result such as recognized text, detected objects or an answer is produced from the input image
  ├─ does the brief call for a help center or list of common questions with expandable answers?  → invoke open-design:faq-page   gate: a page section exists with expandable question and answer entries plus a search or category control
  ├─ does the brief ask for a soft paper toned business report or board memo with a retention chart panel?  → invoke open-design:field-notes-editorial-template   gate: a business report page renders with pastel insight cards and a retention chart panel
  ├─ do shipped code components need to be mapped to their matching figma nodes so design updates propagate automatically?  → invoke open-design:figma-code-connect-components   gate: a mapping file links each code component to its corresponding figma node for automatic sync
  ├─ is there no written ruleset yet for how this project translates figma tokens and naming into code?  → invoke open-design:figma-create-design-system-rules   gate: a rules file capturing tokens naming and lint conventions is written to the project
  ├─ does the workflow need a brand new blank figma or figjam file to start from before anything else runs?  → invoke open-design:figma-create-new-file   gate: a new empty figma or figjam file exists with a returned file key
  ├─ does an existing code page or written description need to become editable screens inside figma?  → invoke open-design:figma-generate-design   gate: figma screens exist built from the design system components matching the source
  ├─ does the codebase design system need to be mirrored into a reusable component library inside figma?  → invoke open-design:figma-generate-library   gate: a component library inside figma contains items matching the codebase design system
  ├─ is there an approved figma frame that now needs to become production code matching it exactly?  → invoke open-design:figma-implement-design   gate: rendered code matches the source figma frame pixel for pixel on comparison
  ├─ is any other figma related step about to run a plugin api script against the canvas?  → invoke open-design:figma-use   gate: a figma plugin api script executes and returns a canvas read or write result
  ├─ does a flutter screen need an animated transition or motion effect added to it?  → invoke open-design:flutter-animating-apps   gate: flutter animation controller or transition code is added to the widget tree
  ├─ does the brief want a newsroom styled chart frame with staggered reveal animation for a line bar or range chart?  → invoke open-design:frame-data-chart-nyt   gate: a chart frame renders with editorial typography and a staggered reveal sequence
  ├─ does the brief want a whiteboard style flow diagram with sticky note nodes and curved connectors?  → invoke open-design:frame-flowchart-sticky   gate: a frame renders sticky note style nodes joined by curved svg connector lines
  ├─ does the brief want a glitchy corrupted looking title frame for a video transition or a cyberpunk hero?  → invoke open-design:frame-glitch-title   gate: a title frame renders with chromatic offset and glitch distortion effects
  ├─ does the brief want a cinematic opening frame with film grain light leaks and letterboxing?  → invoke open-design:frame-light-leak-cinema   gate: a frame renders with letterbox bars film grain and a light leak overlay
  ├─ does the brief want a hero frame with a fluid displacing background behind a quote?  → invoke open-design:frame-liquid-bg-hero   gate: a hero frame renders a fluid displacement shader background under an overlaid quote
  ├─ does a video need a closing frame where the logo assembles from segments with a glowing tagline reveal?  → invoke open-design:frame-logo-outro   gate: a closing sequence frame renders the logo assembling from segments followed by a tagline reveal
  ├─ does the brief want a realistic looking desktop notification banner overlaid on a video or teaser?  → invoke open-design:frame-macos-notification   gate: a frame renders a notification banner with an app icon title and body text
  ├─ is the agent about to build a new user facing interface that needs a strong deliberate visual direction rather than a generic default look?  → invoke open-design:frontend-design   gate: the shipped interface shows a specific typography and layout direction rather than framework defaults
  ├─ does the brief want a full stack showcase or hero page combining cinematic animation with generated media assets?  → invoke open-design:frontend-dev   gate: the shipped page combines animated sequences with generated media assets end to end
  ├─ is the agent building a landing page or app interface that should read as restrained and intentional rather than busy?  → invoke open-design:frontend-skill   gate: the shipped interface follows a restrained composition with deliberate whitespace and hierarchy
  ├─ does the brief want an animated html slide deck for a keynote or embedded talk rather than a static document?  → invoke open-design:frontend-slides   gate: an html slide deck renders with animated transitions between slides and a style preview
  ├─ does a full scrolled page need to be captured as one image for a portfolio case study or audit?  → invoke open-design:full-page-screenshot   gate: a single image file exists containing the entire page from top to bottom
  ├─ does the brief want an uploaded photo turned into an animated figurine style sticker?  → invoke open-design:gif-sticker-maker   gate: an animated figurine style gif file exists rendered from the source photo
  ├─ is the agent building a persuasion structured landing page that needs scroll triggered motion and randomized layout variance across sections?  → invoke open-design:gpt-tasteskill   gate: the page follows an attention interest desire action section order and uses scroll triggered motion with per render layout variance
  ├─ does a basic tween need writing with the core animation library, such as moving fading or staggering elements?  → invoke open-design:gsap-core   gate: a tween call using the core easing duration and stagger options animates the target elements
  ├─ is an animation being added inside a vue or svelte component and does it need to be scoped and cleaned up on unmount?  → invoke open-design:gsap-frameworks   gate: the animation is registered inside the component lifecycle hook and reverted on unmount
  ├─ is an existing animation janky or dropping frames and does it need a performance pass?  → invoke open-design:gsap-performance   gate: the animation now only touches transform properties and avoids layout triggering changes
  ├─ does the animation need a specific registered plugin capability such as dragging flipping splitting text or a custom ease?  → invoke open-design:gsap-plugins   gate: the needed plugin is registered and its specific effect appears in the animation
  ├─ is an animation being added inside a react component and does it need proper ref based cleanup on unmount?  → invoke open-design:gsap-react   gate: the animation uses the react hook with a scoped context that reverts on unmount
  ├─ does an element need to animate in sync with scroll position such as pinning or parallax?  → invoke open-design:gsap-scrolltrigger   gate: the animation is bound to scroll position through a trigger with pinning or scrub configured
  ├─ do several animation steps need to play in a specific sequenced order relative to each other?  → invoke open-design:gsap-timeline   gate: the animations are chained on a single sequence with explicit position offsets
  ├─ does an animation value need clamping mapping interpolating or randomizing with a small helper function?  → invoke open-design:gsap-utils   gate: a utility helper such as clamp or map range is used to compute the animation value
  ├─ does the brief want a diagram that looks sketched by hand rather than precise vector lines?  → invoke open-design:hand-drawn-diagrams   gate: a sketch styled diagram exists as an animated vector graphic with an exportable image
  ├─ does the brief want a custom animated pet spritesheet built from character art or reference images?  → invoke open-design:hatch-pet   gate: a validated animated pet spritesheet file is packaged from the source art
  ├─ does the brief want a bold retro styled quarterly review or roadmap deck with heavyweight headline typography?  → invoke open-design:html-ppt-retro-quarterly-review   gate: a quarterly review deck renders with slab headlines on cream paper sections in the blue and orange palette
  ├─ is an existing image or screenshot low resolution or blurry and does it need sharpening before use?  → invoke open-design:image-enhancer   gate: the output image has higher resolution and sharpness than the input file
  ├─ in codex, does a website need to be implemented to visually match a design image that must first be generated and analyzed?  → invoke open-design:image-to-code-skill   gate: a generated design image exists and the implemented website is compared against it section by section
  ├─ does the brief want reference images of mobile app screens rather than working app code?  → invoke open-design:imagegen-frontend-mobile   gate: a set of mobile screen concept images exists showing consistent hierarchy and palette across screens
  ├─ does the brief want one separate website design reference picture produced for every page section rather than one combined image?  → invoke open-design:imagegen-frontend-web   gate: a separate reference image exists for each page section rather than one combined image
  ├─ does a project asset such as an icon illustration or mockup need to be produced or edited through an image api?  → invoke open-design:imagegen   gate: a picture file is produced or edited by calling the image creation api
  ├─ does a visual asset need creating specifically through the gemini picture creation api rather than another provider?  → invoke open-design:imagen   gate: a picture file is produced through a call to the gemini image creation api
  ├─ does a web or html artifact already exist and now need a final audit and polish pass before sharing it live?  → invoke open-design:impeccable-design-polish   gate: the artifact receives a polish pass with critique notes addressed before the live or share handoff
  ├─ does the current task need an asset that was already captured or uploaded earlier rather than a newly generated one?  → invoke open-design:library-curator   gate: a matching asset is found in the stored registry and applied into the current project
  ├─ does the brief want mobile screens for signing in or authenticating a user?  → invoke open-design:login-flow   gate: a set of mobile screens exists covering the sign in and authentication steps
  ├─ does a hook a framing choice or a pricing presentation need strengthening using behavioral principles?  → invoke open-design:marketing-psychology   gate: the copy or layout is revised citing a specific behavioral principle behind the change
  ├─ does the brief want a clean restrained interface with a warm monochrome palette and no gradients or heavy shadows?  → invoke open-design:minimalist-skill   gate: the shipped interface uses a warm monochrome palette with flat cards and no gradient or heavy shadow styling
  ├─ does a branded word document report or proposal need creating or editing with precise formatting fidelity?  → invoke open-design:minimax-docx   gate: a docx file is produced or edited with the openxml based formatting intact
  ├─ does a branded pdf such as an e-guide or report need generating with a chosen cover style and consistent tokens?  → invoke open-design:minimax-pdf   gate: a pdf file is produced using the token based design system with a selected cover style
  ├─ does the brief want a product showcase image of the interface embedded inside a realistic looking phone or laptop shell?  → invoke open-design:mockup-device-3d   gate: a rendered image shows the real interface embedded on a realistic device screen with lens refraction
  ├─ does a slide deck need generating from a source document combined with generated styled images?  → invoke open-design:nanobanana-ppt   gate: a slide deck is produced combining analyzed document content with generated styled images
  ├─ is a long code generation task at risk of being truncated or filled with placeholder stubs instead of complete output?  → invoke open-design:output-skill   gate: the generated output contains no placeholder markers and is split cleanly across messages if it exceeds one response
  ├─ does an upgrade screen paywall or upsell modal need designing or optimizing for conversion?  → invoke open-design:paywall-upgrade-cro   gate: the upgrade screen or paywall is revised with a stated conversion rationale for the change
  ├─ does text need extracting from an existing pdf or does a simple printable pdf need creating or a form filled?  → invoke open-design:pdf   gate: a pdf is created edited or has its text extracted and the form fields if any are filled
  ├─ does an image or video asset need generating or editing through a large hosted api portfolio of media models?  → invoke open-design:pixelbin-media   gate: the media file is produced or transformed through a call to the hosted api portfolio
  ├─ is a finished ui change about to be merged and does it need a scored design review before that happens?  → invoke open-design:plan-design-review   gate: a review exists scoring each design dimension out of ten with any generic looking ai patterns flagged
  ├─ about to ship one interface design across iOS, Android and the web and need platform specific conventions respected everywhere at once?  → invoke open-design:platform-design   gate: a single design checked against major mobile, desktop and accessibility rule sets before shipping
  ├─ about to produce a single vertical share image meant to grab attention at a glance?  → invoke open-design:poster-hero   gate: one vertical poster style image with a strong focal visual
  ├─ about to build a single file interactive slideshow where each screen is one card navigated with the arrow keys?  → invoke open-design:ppt-keynote   gate: a slideshow file where left and right arrow keys move between one card screens
  ├─ about to generate a brand new presentation file from nothing using a proven generation pipeline?  → invoke open-design:pptx-generator   gate: a newly generated presentation file produced end to end by the pipeline
  ├─ about to check whether an already exported presentation file still matches the original HTML deck it was built from?  → invoke open-design:pptx-html-fidelity-audit   gate: a drift report against the source HTML, followed by a corrected re export
  ├─ about to read or adjust an existing presentation file for an executive briefing or training session?  → invoke open-design:pptx   gate: an existing presentation file opened, adjusted and saved back
  ├─ about to push a follow up commit addressing open review comments or a merge conflict on a pull request?  → invoke open-design:pr-feedback-quality-gate   gate: every open review comment resolved and a read only cross review completed before the push
  ├─ about to visually upgrade an already shipped site or app while guaranteeing none of its existing functionality breaks?  → invoke open-design:redesign-skill   gate: an audit naming generic patterns, followed by visual upgrades verified not to change existing behavior
  ├─ about to start new design work without yet having a written goal, audience, references and style problem to anchor it?  → invoke open-design:reference-design-contract   gate: a short written contract naming the goal, audience, references and the specific style problem to resolve
  ├─ about to publish a single page summarizing what changed in a new release?  → invoke open-design:release-notes-one-pager   gate: a one page release document with every standard section filled in or explicitly marked none
  ├─ about to build a video whose frames are defined as code components so the render can be reproduced exactly?  → invoke open-design:remotion   gate: a video rendered from a component based composition rather than a manually edited timeline
  ├─ about to compare several hosted generation models and expect to swap between them later in the same pipeline?  → invoke open-design:replicate   gate: a generation call naming a specific hosted model version that can be swapped without rewriting the pipeline
  ├─ about to bring stakeholders together to settle one specific product or design decision that is blocking progress?  → invoke open-design:research-decision-room   gate: a single named decision statement the meeting exists to resolve, with an outcome recorded afterward
  ├─ about to lay out a single page resume intended for printing or a PDF download?  → invoke open-design:resume-modern   gate: one A4 sized resume page exportable directly to PDF
  ├─ about to sign off on a motion or animation implementation before it ships?  → invoke open-design:review-animations   gate: a review verdict that starts from flagged and only turns to approved after specific craft issues are addressed
  ├─ about to capture what is currently on screen on the local machine, a specific window or a chosen pixel region?  → invoke open-design:screenshot   gate: an image file of the requested screen area saved locally
  ├─ about to produce polished browser captures of a live web page for a store listing, hero section or changelog entry?  → invoke open-design:screenshots-marketing   gate: a set of browser automated images sized for the specific marketing placement
  ├─ about to assemble an interface from a library of pre built accessible components instead of styling one from scratch?  → invoke open-design:shadcn-ui   gate: a screen composed from library components rather than hand rolled markup
  ├─ about to write low level graphics code that runs per pixel on the GPU for a hero visual?  → invoke open-design:shader-dev   gate: a compiled shader program producing the intended visual effect on the GPU
  ├─ about to build an animated GIF that will be uploaded into a chat tool with a strict file size limit?  → invoke open-design:slack-gif-creator   gate: an animated GIF that passes a file size validator before upload
  ├─ about to put together a presentation deck specifically for a sales pitch, a kickoff brief or a showcase of a design system?  → invoke open-design:slides   gate: a presentation file tailored to that specific pitch, brief or showcase purpose
  ├─ about to mock up a realistic forum post card complete with a vote rail and comment count for a video overlay?  → invoke open-design:social-reddit-card   gate: a rendered post card showing vote count and comment count in that forum style
  ├─ about to mock up a now playing music card with album art and a playback progress bar?  → invoke open-design:social-spotify-card   gate: a rendered music card showing album art, a progress bar and playback controls
  ├─ about to mock up a realistic short form post card showing likes, reposts and view counts?  → invoke open-design:social-x-post-card   gate: a rendered post card displaying likes, reposts and view counts
  ├─ about to pick fonts, spacing, shadows and card structure for a brand new interface and worried the result will look like a generic default template?  → invoke open-design:soft-skill   gate: specific font, spacing, shadow and card structure choices recorded, not left at framework defaults
  ├─ about to generate a short cinematic video clip from a text description through a hosted video model?  → invoke open-design:sora   gate: a rendered short video clip returned from the hosted model call
  ├─ about to turn a finished script into a one shot narrated audio file using a fixed set of built in voices?  → invoke open-design:speech   gate: an audio file of narration synthesized once from a fixed built in voice
  ├─ about to compare a built interface against its original brief and cycle through critique and adjustment before shipping?  → invoke open-design:stitch-loop   gate: at least one critique then adjust pass recorded between the built UI and the original brief before ship
  ├─ about to write a governing design document that future UI work will be checked against?  → invoke open-design:stitch-skill   gate: a DESIGN.md file capturing typography, color and layout rules for future work to follow
  ├─ about to design a native Apple platform app interface and want it checked on five separate quality dimensions?  → invoke open-design:swiftui-design   gate: a native app screen scored across five separate design dimensions before it is considered done
  ├─ about to build a single interactive HTML presentation with switchable color themes and clickable hotspots on each slide?  → invoke open-design:swiss-creative-mode-template   gate: a single HTML file presentation where the palette can be switched and hotspots can be clicked
  ├─ about to turn user research findings into a story first deck with donut chart breakdowns rather than a plain slide per finding list?  → invoke open-design:swiss-user-research-video-template   gate: a research deck containing at least one donut chart breakdown and a defined narrative order
  ├─ about to rely on an older design judgment behavior because the current default version would change the output?  → invoke open-design:taste-skill-v1   gate: the older, pinned version explicitly selected instead of the current default
  ├─ about to infer a design direction from a brief and ship a landing page, portfolio or redesign without a fixed template to follow?  → invoke open-design:taste-skill   gate: a shipped interface whose direction was inferred from the brief rather than copied from a fixed template
  ├─ about to apply a consistent font and color theme across a batch of different artifact types like slides, docs and a landing page?  → invoke open-design:theme-factory   gate: the same named theme applied consistently across every artifact type in the batch
  ├─ about to build an interactive 3D scene that runs directly in the browser?  → invoke open-design:threejs   gate: a browser scene rendering 3D geometry with working camera controls
  ├─ about to build many small separate UI pieces over time and need them to stay visually coherent with each other?  → invoke open-design:ui-skills   gate: a shared constraint list that every new small UI piece is checked against
  ├─ about to look for advanced UI and UX templates and a search workflow inside this catalog entry expecting them to be fully bundled?  → invoke open-design:ui-ux-pro-max   gate: an explicit notice that only the catalog listing exists here with no bundled templates or search workflow
  ├─ about to generate a short piece of background music or a jingle through a queued generation job?  → invoke open-design:venice-audio-music   gate: a completed music generation job retrievable as an audio file
  ├─ about to stream live spoken responses for a conversational agent and need to choose among several voice models and audio formats?  → invoke open-design:venice-audio-speech   gate: a chosen voice model and audio format producing streamed conversational speech
  ├─ about to edit an existing image by upscaling it or removing its background rather than generating a new one?  → invoke open-design:venice-image-edit   gate: the same source image returned edited, upscaled or with its background removed
  ├─ about to generate a brand new image from a text prompt and need to pick a specific visual style?  → invoke open-design:venice-image-generate   gate: a newly generated image produced from a prompt and a chosen style
  ├─ about to generate a new video clip or transcribe the audio of an existing one?  → invoke open-design:venice-video   gate: either a generated video file or a text transcript of an existing video
  ├─ about to reveal a quote word by word in a video intro and want a light trail following the reveal?  → invoke open-design:vfx-text-cursor   gate: a video intro where each word appears in sequence trailed by a light effect
  ├─ about to save an online video locally for offline editing or archival rather than streaming it live?  → invoke open-design:video-downloader   gate: a local video file saved in the requested format and quality
  ├─ about to build a continuous frame by frame animation that starts playing automatically without a user click?  → invoke open-design:video-hyperframes   gate: a frame animation composition that begins playing on its own when loaded
  ├─ about to ship a complex embeddable HTML artifact built with a component framework and a utility CSS system, following an official reference workflow?  → invoke open-design:web-artifacts-builder   gate: an embeddable HTML artifact built from framework components and utility classes matching the reference workflow
  ├─ about to reproduce an existing site design and interaction from scratch and tempted to skip straight to guessing its code?  → invoke open-design:web-clone   gate: the real source of the target site retrieved before any rebuild code is written
  ├─ about to review already written interface code for layout, typography, color, motion and accessibility compliance?  → invoke open-design:web-design-guidelines   gate: a code review pass checking layout, typography, color, motion and accessibility against a written guideline set
  ├─ about to turn a full year of reading activity into a shareable vertical video recap?  → invoke open-design:weread-year-in-review-video-template   gate: a vertical video file summarizing a year of reading activity, ready to export
  ├─ about to build a theme or site for a specific publishing platform and need the official design tokens and component patterns applied consistently?  → invoke open-design:wpds   gate: the official design tokens and component patterns applied consistently across the theme or site
  ├─ about to publish written documentation or prose and want its voice, tone and style checked against a handbook first?  → invoke open-design:writing-guidelines   gate: a prose review noting voice, tone and style issues against a written handbook before publishing
  ├─ about to pull highlight moments out of a longer source video and export them as captioned clips?  → invoke open-design:youtube-clipper   gate: one or more exported clips cut from the source video with captions burned in or attached
```

**Drift:** every route above targets `open-design:<slug>` in the separately-installed plugin. If a slug no longer resolves there, that route is a silent no-op. Never substitute a nearby-sounding skill: a broken route means this Flow needs an update, not that you may improvise.

## Attribution

Skills routed by this Flow come from **open-design** (https://github.com/nexu-io/open-design). This overlay bundles none of them; it installs the routing only.

Routing (this FLOW.md) by Flowy, CC-BY-SA-4.0.
